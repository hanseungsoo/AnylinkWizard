<?xml version="1.0"?>
<nodes xmlns="http://www.tmaxsoft.com/xml/ns/jeus" version="7.0">
{%- for node in nodeList %}
    <node>
        <name>{{ node.name }}</name>
        <host>{{ node.host }}</host>
        <java>
            <port>{{ node.port }}</port>
            <use-ssl>{{ node.use_ssl }}</use-ssl>
        </java>
    </node>
{%- endfor %}
</nodes>
